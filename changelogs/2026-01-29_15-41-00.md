# Changelog

## [Unreleased]

### Removed
- Numerous test environment files under `tests/generated_envs/` as they are no longer needed or have been replaced.

### Changed
- Updated configuration to exclude additional directories (`\.venv`, `venv`) from code formatting and sorting tools (`black` and `isort`).