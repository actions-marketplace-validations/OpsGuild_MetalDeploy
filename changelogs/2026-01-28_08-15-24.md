There are no changes to generate a changelog for, as the diff only shows a change in whitespace (the addition of a newline at the end of the file). The content of the changelog itself remains the same. 

If you'd like to generate a changelog for a different diff, please provide it.