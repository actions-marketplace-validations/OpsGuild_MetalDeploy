# Changelog

## [Unreleased]

### Added
- Exclusion of `generated_envs` directories from version control and pre-commit checks to prevent unnecessary checks and commits.

### Changed
- Updated pre-commit configuration to exclude files within `tests/` directory that are in `generated_envs/` subdirectories, improving pre-commit hook performance.
